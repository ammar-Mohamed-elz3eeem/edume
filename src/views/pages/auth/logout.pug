extends ../../layout.pug
block content
  .text-center.d-flex.flex-column.gap-4
    h3 are you sure you want to sign out
    form.d-flex.align-items-center.flex-row.gap-4.justify-content-center(action="/auth/logout" method="POST")
      button(type='submit') Yes
      a(href="/auth")
        button(type='button') No 